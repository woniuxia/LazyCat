# 项目管理思源页面关联弹窗默认列表与本地过滤优化设计

日期：2026-04-02

> 目标：优化项目管理中“项目任务关联页面”弹窗的默认行为。弹窗打开后应直接展示当前项目有效存储位置下的全部文档；项目未配置专属位置时自动回退全局默认位置；输入内容后仅做本地即时过滤。同时保留“扩展到全库”作为手动触发的次级入口，不破坏本地优先体验。

## 1. 背景与目标

### 1.1 当前现状

- 页面关联弹窗位于 `apps/desktop/src/components/PmPanel.vue`。
- 当前交互以“搜索”为中心：
  - 打开弹窗后默认结果为空。
  - 会尝试把工作项标题预填到搜索框。
  - 关键词达到 2 个字符后才会自动发起远端搜索。
  - 顶部同时存在 `搜索` 与 `扩展到全库` 两个按钮。
- 目录树数据已可通过 `tool:pm:siyuan-directory` 读取，并缓存在前端 `siyuanDirectory` 状态中。
- 现有 `apps/desktop/src/utils/pmSiyuan.ts` 已承担 PM 思源相关纯函数逻辑，并已有 `pmSiyuan.test.ts` 测试。

### 1.2 本轮目标

1. 弹窗打开后默认展示“当前位置下的全部文档”，而不是空结果。
2. 有效位置解析规则固定为：`项目专属位置 > 全局默认位置`。
3. 项目未配置专属位置时，自动回退到全局默认位置。
4. 输入框行为改为本地即时过滤，不再依赖回车，也不再默认请求思源搜索。
5. 保留“扩展到全库”作为手动备用入口。

### 1.3 非目标

1. 不重做思源目录获取协议。
2. 不改动 Rust 侧已有 `siyuan-search-pages` 搜索接口的协议。
3. 不在本轮新增目录缓存表或单独的页面列表接口。
4. 不把页面关联弹窗改造成复杂的双栏选择器或树形选择器。
5. 不改动页面创建逻辑的核心行为。

## 2. 已确认决策

1. 主路径采用“本地列表优先”，不再把远端搜索作为默认流程。
2. 弹窗默认列表来源于当前有效位置对应的目录子树。
3. 输入框仅做本地即时过滤，不再要求最少 2 个字符。
4. 若无项目专属位置，则自动回退到全局默认位置。
5. “扩展到全库”保留为次级入口，但必须手动触发。

## 3. 交互设计

### 3.1 弹窗打开行为

1. 点击“关联主页面”或“添加附加页面”后，先解析当前有效位置：
   - 优先使用项目专属位置
   - 若项目未配置，则回退全局默认位置
2. 若目录缓存为空，先加载目录后再展示列表。
3. 若目录缓存已存在，立即展示缓存结果，并在后台静默刷新目录。
4. 打开弹窗时搜索框默认留空，不再自动填入工作项标题。
5. 默认结果集为“当前位置文档列表”，展示该位置节点本身与其全部后代文档。

### 3.2 输入过滤行为

1. 搜索框改为“过滤输入框”，placeholder 建议调整为：
   `输入标题或路径过滤当前列表`
2. 用户输入后立即对当前结果源执行本地过滤：
   - 匹配 `docTitle`
   - 匹配 `docHpath`
   - 匹配 `docPath`
3. 清空输入后，列表恢复为当前结果源的完整结果。
4. 本地过滤不触发任何远端请求。

### 3.3 扩展到全库行为

“扩展到全库”保留，但降级为手动备用入口：

1. 仅在用户主动点击按钮时发起远端搜索。
2. 若当前关键字长度小于 2，继续沿用现有提示规则，要求输入至少 2 个字符。
3. 远端搜索成功后，将当前结果源切换为“全库结果”。
4. 全库结果展示期间，顶部应提供一个明确的“返回当前位置列表”入口。
5. 返回当前位置列表后，恢复默认本地列表模式；当前输入框内容保留，并继续在当前位置列表上做本地过滤。

说明：

- 默认打开弹窗时不进入全库模式。
- “扩展到全库”只是补充能力，不应抢占主流程。
- 全库模式的后续输入不自动再次请求思源；只有再次点击按钮时才重新远端搜索。

### 3.4 文案与状态

顶部提示文案从“当前搜索范围”调整为“当前列表范围”，并根据模式展示不同内容：

- 当前位置模式：
  `当前列表范围：研发笔记 · /项目页（共 18 篇）`
- 全库模式：
  `当前列表范围：全部已打开笔记本（共 20 篇）`

空状态分为三类：

1. **未配置位置**
   - 文案：`当前未配置项目专属位置或全局默认位置，请先设置思源存储位置。`
2. **当前位置无文档**
   - 文案：`当前位置暂无可关联文档，可以直接新建页面。`
3. **过滤后无结果**
   - 文案：`当前过滤条件下没有匹配文档。`

## 4. 数据与状态设计

### 4.1 现有状态调整

`PmPanel.vue` 中的页面关联弹窗状态将从“搜索态”调整为“列表态 + 可选全库搜索态”。

建议新增或重构以下前端状态：

```ts
const siyuanPageDialogMode = ref<"primary" | "extra">("primary");
const siyuanPageFilterKeyword = ref("");
const siyuanPageResultSource = ref<"location" | "all">("location");
const siyuanPageLocationResults = ref<PmSiyuanPageRef[]>([]);
const siyuanPageAllResults = ref<PmSiyuanPageRef[]>([]);
const siyuanPageSearchingAll = ref(false);
```

说明：

- `siyuanPageLocationResults`：当前位置完整列表
- `siyuanPageAllResults`：最近一次全库搜索结果
- `siyuanPageResultSource`：当前展示的数据源
- 当前展示列表通过 `computed` 基于“当前结果源 + 本地过滤关键字”派生

### 4.2 有效位置解析

继续复用现有 `resolvePmSiyuanEffectiveLocation()` 纯函数，确保逻辑唯一：

```ts
projectLocation ?? globalLocation ?? null
```

本轮不新增第二套位置解析逻辑，避免后续项目设置与弹窗表现不一致。

## 5. 页面列表构建方案

### 5.1 复用目录树缓存

页面关联默认列表不新增后端接口，直接从已有 `siyuanDirectory` 目录树派生。

理由：

1. 当前目录数据结构已足够描述文档层级。
2. 本轮目标是交互优化，不是重建数据通路。
3. 复用 `pmSiyuan.ts` 纯函数更容易测试与维护。

### 5.2 纯函数拆分

建议在 `apps/desktop/src/utils/pmSiyuan.ts` 新增以下能力：

1. `collectPmSiyuanPagesForLocation(notebooks, location)`
   - 输入目录树与有效位置
   - 输出该位置下的全部文档列表
2. `filterPmSiyuanPages(pages, keyword)`
   - 对页面列表做本地过滤
3. 视需要补充内部 helper：
   - 查找目标笔记本
   - 在目录树中定位目标节点
   - 拍平目标子树为 `PmSiyuanPageRef[]`

### 5.3 子树提取规则

1. 若位置指向笔记本根目录：
   - 返回该笔记本下全部文档
2. 若位置指向某个父文档：
   - 返回该父文档本身
   - 再追加该父文档下全部子文档
3. 结果按目录遍历顺序输出，保证与用户对目录结构的认知一致
4. 结果按 `docId` 去重，避免目录刷新或结构异常时重复展示

## 6. 组件改动设计

### 6.1 `PmPanel.vue`

需要调整的点：

1. 打开弹窗时不再自动填充标题并触发搜索
2. 调用 `ensureSiyuanDirectoryLoaded()` 后，构建当前位置完整列表
3. 将结果展示改为读取 `computed` 派生列表
4. 删除默认“搜索”按钮
5. 保留 `扩展到全库` 按钮，并增加 `返回当前位置列表` 按钮
6. 提示文案从“搜索范围”改成“列表范围”
7. 空状态根据“未配置位置 / 无文档 / 过滤无结果”区分显示

### 6.2 `pmSiyuan.ts`

新增页面列表提取与过滤纯函数，并保持实现无副作用、无 Vue 依赖。

### 6.3 Rust 后端

本轮不改动后端数据结构，只复用已有：

- `tool:pm:siyuan-directory`
- `tool:pm:siyuan-search-pages`

其中：

- `siyuan-directory` 用于默认列表
- `siyuan-search-pages` 仅用于手动“扩展到全库”

## 7. 数据流

```text
打开弹窗
  -> 解析有效位置（项目 > 全局）
  -> 确保目录已加载
  -> 从目录树提取当前位置全部文档
  -> 渲染默认列表
  -> 用户输入
  -> 本地过滤当前结果源

点击“扩展到全库”
  -> 校验关键字长度
  -> 调用 siyuan-search-pages(searchAll=true)
  -> 保存全库结果
  -> 切换展示源为 all
  -> 本地过滤全库结果

点击“返回当前位置列表”
  -> 切换展示源为 location
  -> 继续使用当前输入做本地过滤
```

## 8. 错误处理

### 8.1 默认列表阶段

1. 未配置有效位置时，不报错弹 toast，改为在弹窗主体给出明确提示
2. 目录加载失败时，沿用现有错误提示；若有旧缓存则优先保留旧缓存列表
3. 若有效位置在最新目录中不存在：
   - 提示：`当前默认位置已不存在，请重新选择位置`

### 8.2 全库搜索阶段

1. 继续复用现有后端校验：
   - 关键词长度需在 2 到 50 个字符之间
2. 全库搜索失败时：
   - 保持当前位置列表不丢失
   - 不强制切换到空的全库模式

## 9. 测试设计

### 9.1 单元测试

在 `apps/desktop/src/utils/pmSiyuan.test.ts` 中补充：

1. 根目录位置能提取笔记本下全部文档
2. 父文档位置能提取父节点自身与全部子节点
3. 无项目位置时可通过 `resolvePmSiyuanEffectiveLocation()` 回退到全局位置
4. 页面列表过滤支持标题、`hpath`、`path`
5. 页面列表过滤关键字为空时返回原列表

### 9.2 集成验证

1. 弹窗打开默认展示当前位置全部文档
2. 项目未配置专属位置时自动回退全局默认位置
3. 输入后即时过滤，不需要回车
4. 清空输入后恢复完整列表
5. 点击“扩展到全库”后可切换到远端结果
6. 点击“返回当前位置列表”后恢复本地列表模式

### 9.3 命令验证

- `pnpm test src/utils/pmSiyuan.test.ts`
- `pnpm typecheck`
- `pnpm --filter @lazycat/desktop build:web`

## 10. 风险与取舍

1. 默认列表依赖目录缓存，首次打开弹窗时可能需要一次目录加载
2. 目录树是层级结构，页面列表是平面结构，提取规则必须保持稳定，否则容易出现“根页漏掉 / 子页重复”问题
3. 若继续保留全库按钮，就必须清晰标识“当前正在看的是哪个结果源”，避免用户误以为输入一直在全库中实时搜索

## 11. 实施摘要

本轮采用“本地列表优先，全库搜索兜底”的轻量方案：

1. 默认打开即展示当前位置全部文档
2. 输入仅做本地即时过滤
3. 项目位置缺失时自动回退全局默认位置
4. 保留“扩展到全库”作为手动次级入口
5. 不新增后端接口，优先复用现有目录树和纯函数能力
