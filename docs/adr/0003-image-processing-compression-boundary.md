# 图片处理的压缩能力边界

---
status: accepted
---

图片处理新增独立的压缩动作，压缩模式固定源图片格式和像素尺寸，只调整 JPEG/AVIF 质量或 PNG 无损压缩级别；WebP 暂保持无损编码。这样转换与压缩的用户语义和接口契约保持分离，同时继续使用现有离线编码能力，避免为有损 WebP 引入新的编解码依赖和维护边界；压缩结果不强行保证变小，而是如实返回实际体积变化。
