# 上传后命令采用环境级逐命令超时

上线包为每个环境保存一个上传后命令最长运行时间，并对前端、后端命令分别计时；相比为每个目标重复配置，这能保持环境交付策略一致，同时不会让前一个命令消耗后一个命令的运行预算。命令超时后关闭当前 SSH 通道并保留服务器文件已提交的事实，终态沿用可重试的上传后命令失败；由于关闭通道不能证明远端命令及其子进程已经终止，结果必须明确标记远端进程状态未知，且不得回滚已上传文件。
