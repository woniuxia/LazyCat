# 关注事项独立于 Todo 状态机

关注事项采用独立实体、持久化和生命周期，不作为 Todo 分类、执行人组合或状态扩展实现。两者可以复用人员名录、提醒基础设施和界面骨架，但关注状态、外部结果、滚动复查和进展时间线由关注事项领域自行维护；这是因为 Todo 的完成、逾期、周期生成和提醒清理表达的是用户执行任务，将关注行为塞入同一状态机会混淆责任与结果。首版逻辑复用现有人员名录并保留物理表名，同时在关注事项中保存责任人姓名快照，暂不为此迁移 Todo 数据。
