# LazyCat 工具领域

记录 LazyCat 开发者工具中需要保持一致的用户领域术语。

## 离线微工具路线图

**离线核心路径**：
微工具从用户输入到主要输出的完整路径，在断网、无远程认证、CDN 和在线服务时仍可完成。可选联网增强不属于核心路径。

**维护边界**：
一个微工具在输入输出、格式和依赖上的明确支持范围，以及不在范围内的扩展方向。

**不做**：
在实现前确认核心路径无法离线完成，或问题无法收敛为单点微工具时，对该候选不进入实现。

**停止扩展**：
现有 MVP 仍可用，但新需求超出既定维护边界时，保留现有能力而不继续增加范围。

**退出**：
核心能力因安全、许可证、系统兼容性或依赖失效而无法继续维护时，将该工具从可维护集合移除；低使用量本身不构成退出理由。

**异常堆栈整理器**：
面向常见 JavaScript/TypeScript、Java、Python 和 Rust 异常堆栈的本地整理工具，提取异常类型、消息、文件路径和行列号，生成排查摘要。

**文本编码与换行诊断器**：
面向本地文本和代码文件的编码、BOM、换行符及不可见字符检查工具，并提供明确的常见编码转换预览。

## HTTP 状态码工具

**状态码条目**：HTTP 状态码工具中可被列表或搜索结果选中的一条状态码记录，包含码值、标准名称、中文含义、用途和排查相关信息。

**详情展开**：在状态码列表的原行位置展开该状态码的实战信息，不离开当前列表上下文。

**实战型详情**：状态码条目的扩展说明，包含语义解释、常见场景、触发原因、排查建议和相关响应头提示。

**多重详情展开**：同一列表中允许多个状态码条目同时保持展开；用户收起某一条时不影响其他条目。

**常见标准状态码**：纳入 HTTP 标准定义且在开发排查中常用的状态码集合；不包含厂商或代理实现自定义的非标准码。

**未知状态码**：输入的三位数不在收录的标准状态码条目中时，仅根据 1xx–5xx 段位显示通用分类，并明确标注具体含义未定义；不得生成具体语义或排查建议。

**通用分类提示**：针对未知状态码显示的独立提示区域，说明其所属 1xx–5xx 段位及“具体含义未定义”，不作为正式状态码条目参与列表或详情展开。

## JSON 数组过滤

**JSON 输入文档**：用户提供给 JSON 工作台、作为后续处理唯一事实来源的 JSON 内容。

**数组路径**：JSON 输入文档中实际解析为数组值的 JSONPath；数组过滤器展示并使用按深度优先顺序发现的首个可用数组路径。

**对象数组**：由 JSON 对象元素组成的数组，空数组也归入该形态，是数组过滤器当前支持的目标数组形态。

**属性候选**：对象数组所有元素顶层 key 的按首次出现顺序去重集合；勾选后用于构造数组过滤结果。

**缺失属性**：某个对象元素中不存在的已选属性；数组过滤结果中省略该 key，不用默认值或 `null` 代替。

**默认全选**：数组路径确定后，属性候选初始全部处于选中状态的交互规则。

**空属性投影**：没有选中任何属性时，对象数组中每个元素仍对应结果中的一个空对象，数组长度保持不变。

**结果属性顺序**：数组过滤结果中每个对象沿用对应输入对象的原始 key 顺序，只移除未选属性。

**自动解析**：JSON 输入文档停止编辑约 300ms 后自动解析；空输入清空派生状态，非法输入清空旧结果并显示错误，合法输入立即重新计算首个可用数组。

**解析失败状态**：JSON 输入文档无法解析时保留输入文本，但清空数组路径、属性选择和数组过滤结果，直到下一次解析成功。

**无对象数组空态**：JSON 输入文档语法正确但没有可过滤对象数组时的结果状态；显示说明性提示，不生成路径或结果。

**不支持数组**：包含 primitive、`null`、嵌套数组或对象/非对象混合元素的数组，不作为数组过滤目标。

**首个可用数组路径**：按 JSON 文档深度优先顺序遇到的第一个对象数组路径；primitive/混合数组跳过，空数组可作为首个可用路径。

**数组过滤结果**：基于 JSON 输入文档中首个可用数组路径和用户选择的属性生成的只读根数组投影；不会反向修改 JSON 输入文档。

## 测试邮件助手

**邮件正文模板**：测试邮件助手中可被选择、编辑和保存的一份邮件正文内容；正文中的占位符用于填写字段并生成预览。

**系统名**：用于标识被测试系统的名称，来自 Word 模板变量 `应用系统名称`。

**需求内容**：测试邮件助手中的多行需求文本，来自 Word 模板变量 `功能需求内容`。

**需求号**：从需求内容每个非空行中识别出的需求标识；每行在序号点号后跳过分隔空白，取到需求号后的第一个空白为止，多行需求号以顿号连接。

**测试报告文件名**：由生成日期、固定文本“测试报告”、系统名和需求号组成的 Word 报告名称；同一名称再次生成时覆盖已有报告。

## 关注事项

**关注事项**：由他人负责推进、用户只需持续确认进展和结果的事情；它不表示用户本人需要执行的任务。
_Avoid_：任务、待办事项、委派任务

**外部进展**：责任人所负责事情的实际推进状态或最终结果；它独立于用户是否继续关注。
_Avoid_：关注状态

**关注状态**：用户与关注事项之间仍在关注或已结束关注的关系状态；结束关注不等于外部事情已经完成。
_Avoid_：完成状态、任务状态

**进展记录**：用户每次确认关注事项后追加的一条带时间信息的反馈事实；新的进展记录不覆盖历史记录。
_Avoid_：任务日志、操作日志

**复查时间**：用户计划再次确认关注事项进展的时间；仍在关注的事项必须有明确的下一次复查时间。
_Avoid_：截止时间、完成时间

**责任人**：负责推进关注事项所指事情的人；用户是关注者，不因记录该事项而成为责任人。
_Avoid_：关注者、执行用户

**人员名录**：可被任务选为执行人、也可被关注事项选为责任人的本地人员集合；具体角色由引用该人员的事项决定。
_Avoid_：执行人列表、责任人列表

**确认结果**：用户确认外部事情已经完成或已经取消，并据此结束关注；它记录的是已知外部结果。
_Avoid_：完成任务

**结束关注**：用户停止跟踪关注事项；外部事情可以仍未完成或结果未知。
_Avoid_：完成、确认结果

**待复查**：关注事项已到复查时间但尚未产生新的确认结果或进展记录的状态；它不表示责任人的工作已经逾期。
_Avoid_：逾期、超期任务

**滚动复查**：关注事项尚未结束时，通过设置新的复查时间延续同一个事项；它不生成新的事项实例。
_Avoid_：周期事项、重复任务

**预计完成时间**：责任人预计完成外部事情的时间；它可以为空，并且独立于用户必须设置的复查时间。
_Avoid_：复查时间、提醒时间

**外部期限已到**：预计完成时间已经到达但尚未确认外部结果的提示；它描述外部承诺，不等同于用户未按时复查。
_Avoid_：待复查、任务逾期

**继续关注**：用户追加进展记录并设置新的复查时间，使同一个关注事项继续保持关注中的操作。
_Avoid_：新建下一期、完成本次

**稍后提醒**：用户临时推迟当前提醒再次出现的时间；它不改变关注事项正式记录的复查时间。
_Avoid_：修改复查时间

**重新关注**：用户让已结束的关注事项重新进入关注状态，并为其设置新的复查时间；历史进展保留，当前外部结果恢复为未知。
_Avoid_：恢复任务、重新创建

**聚合复查提醒**：同一轮发现多个待复查事项时生成的一条汇总提醒；它只引导用户进入待复查列表，不批量推迟各事项的提醒。

**快速输入模板**：关注事项中用于辅助填写进展或结果的一段全局共享文本；点击后追加到当前输入，按全局使用频率排序，并可新增、编辑或删除。
_Avoid_：批量稍后提醒

## 工具标签页状态

**任务清单标签页**：承载用户本人需要执行的 Todo 工作流的独立工具标签页；其页面内容的主视图称为“我的任务”。

**关注事项标签页**：承载用户跟踪外部责任人进展的独立工具标签页；它与任务清单标签页并列存在，拥有独立页面状态和生命周期。

**任务页切换**：位于任务清单标签页和关注事项标签页左上角的跨标签页导航；点击后激活已存在的目标标签页，不改变两个领域各自的数据和页面状态。

**工具标签页**：应用主界面中代表一个已打开工具的独立导航项；标签页仍存在时，该工具的工作上下文仍属于当前会话。

**标签页状态保留**：用户从一个工具切换到另一个工具后，仍打开的工具应保留切换前的输入内容、滚动位置和局部操作上下文；关闭标签页才结束该工具页面的会话状态。应用重启后的恢复不属于该术语默认含义。

**标签页滚动上下文**：工具标签页外层页面视口及页面实例内部滚动区域的当前位置；切换标签页时随页面上下文保留，关闭标签页时结束。它不表示应用重启后的滚动快照恢复。

**标签页失活**：工具标签页仍处于打开状态，但暂时不是当前可交互页面；其用户工作状态继续保留，页面级交互和资源活动是否暂停按生命周期契约处理。

**标签页首次呈现**：一个标签页页面上下文第一次向用户展示的阶段；首次呈现时允许使用引导性的视觉动效，若用户在动效完成前离开，后续从失活状态恢复时直接进入可用状态，不等待或重播未完成动效。

**首页内容即时可用**：首页从缓存恢复或内容发生变化时，已有和新出现的工具内容都应立即可见并可交互，不等待整页视觉动效完成；系统要求减少动态效果时，首次呈现也直接可用。

**应用级长任务**：启动后独立于当前工具标签页继续运行的异步工作；切换或关闭标签页不等于取消，用户必须显式终止，后续重新打开相关工具时仍可查看其当前状态和最终结果。

**页面级视图**：负责展示工具状态和承载当前交互的页面上下文；其失活或销毁不应自动改变独立运行的应用级长任务。

**统一标签页缓存**：首页、设置、离线手册和普通工具等所有可重新打开的标签页遵循同一状态保留和关闭销毁契约，不因工具类型不同而改变用户对页面连续性的预期。

**Tab 页面容器**：一个已打开标签页对应的独立页面上下文；它拥有该标签页的 UI 状态和生命周期，标签页关闭时连同其缓存状态一起结束。

**单工具标签页**：同一个工具标识在同一时间最多对应一个打开的标签页；再次打开该工具时激活已有标签页，关闭后重新打开才创建新的页面上下文。

**页面级单次操作**：由用户在工具页面发起、预期产生一次结果的异步操作；切换标签页不取消或重复启动，页面关闭后不再向已销毁页面回写结果。

## 上线包

**日志跟随**：上线包的每个日志区独立保持最新日志可见的浏览状态；用户离开底部时暂停，回到底部时恢复。
_Avoid_：强制滚动、全局日志滚动

**上线包运行记录**：一次上线包运行在本机保留的历史事实，归属于当时的项目和环境，包含运行结果以及各日志区按已声明容量边界保留的输出。
_Avoid_：项目总日志、当前运行缓存

**未完成运行记录**：上线包运行已经开始并产生持久化日志，但因应用异常退出而没有形成最终运行结果的历史记录。
_Avoid_：运行失败、已中止

**运行日志截断**：单个日志区超过持久化容量边界后，保留启动部分和最新部分，并明确记录中间被丢弃的字节数；它不改变上线包运行结果。
_Avoid_：日志保存失败、静默丢弃

**上线包原地解锁**：服务器交付需要读取已绑定凭据而 Vault 处于锁定状态时，用户在当前上线包确认上下文中输入主密码并恢复交付流程，不跳转到密码管理工具。
_Avoid_：跳转解锁、单次凭据授权

**Vault 解锁会话**：主密码校验成功后建立的应用级密码库访问状态，由现有自动锁定策略统一结束；从上线包原地解锁与从密码管理工具解锁具有相同效力。
_Avoid_：上线包专用会话、上传临时会话

**全局中止**：上线包运行中由用户发起的整任务中止请求，覆盖当前前端构建、后端构建和服务器上传阶段；它不是某个单独目标的中止。

**目标级结果**：前端和后端各自实际执行到的状态与错误；全局终态只做总体聚合，不覆盖已完成目标、已中止目标和未执行目标的事实。

**远端提交边界**：服务器上传中所有选定目标完成正式切换的整体提交阶段边界；整体提交完成前的中止需要尽力回滚已切换目标并清理临时目标，整体提交完成后只停止尚未执行的后续步骤，不回滚已提交文件。

**本地归档提交边界**：本地归档从临时 stage 切换为最终归档目录的提交边界；在此之前的中止需要清理 stage，此后保留已提交归档，不因迟到的中止请求删除或覆盖它。

**中止请求**：用户针对当前上线包运行发出的停止意图；它先驱动运行协作停止，只有后端完成收口后才形成最终的“已中止”状态。

**中止清理异常**：用户中止已经生效，但进程、连接或临时目标未能完整终止或清理的结果；必须保留中止事实，同时给出错误上下文和人工检查路径。

**迟到中止**：任务已经完成最终收口后才到达的同一运行中止请求；它不改变既有终态、产物或远端结果，只返回未请求中止。

**敏感启动状态**：上线包确认与预检期间暂存的私钥口令、主机探测令牌、预检令牌和远端覆盖确认状态；它只服务于当前一次启动，启动收口、取消或异常后必须清理。

**中止入口**：上线包页面和启动确认弹窗中发起全局中止请求的用户操作位置；全局通知只展示状态和结果，不直接改变运行状态。

**后置步骤中止**：服务器文件已提交后，用户中止当前上传后命令或健康检查的结果；不回滚已产生的远端副作用，也不启动其后的后置步骤。

**上传后命令**：服务器文件提交完成后，针对单个交付目标执行的一次性远端部署钩子；它必须在有限时间内返回退出结果，不承载常驻服务或持续日志会话。

**常驻远端进程**：需要脱离本次 SSH 命令会话持续运行的服务器进程；它应由远端服务管理器负责生命周期，不属于上传后命令的执行结果。

**后置命令超时**：上传后命令超过本次环境允许的最长执行时间后形成的独立失败事实；服务器文件仍视为已提交，远端进程是否已停止必须单独说明。

**远端进程状态未知**：本地已超时或中止并关闭 SSH 通道，但无法确认远端命令及其子进程是否完全终止的结果；不得将其表示为已清理。

**上传后命令日志上限**：单条上传后命令允许写入本地运行日志的最大字节量；超过后继续消费远端输出但只记录截断计数，不改变命令本身的执行语义。

**构建进程树中止**：用户中止前端或后端构建时，连同 PowerShell 父进程及其子进程一起终止；单独结束父进程不算中止完成。

## 图片处理

**图片处理**：LazyCat 中面向单张本地图片的转换、压缩、尺寸调整和裁剪工具；其核心路径在本地完成，不依赖在线服务。

**转换模式**：以改变图片格式、像素尺寸或裁剪区域为主要目的的图片处理方式；输出文件可以与源图片使用不同格式或尺寸。

**压缩模式**：以降低图片文件体积为主要目的的图片处理方式；默认保持源图片的格式和像素尺寸，只改变编码参数，结果不保证一定小于源文件。

**文件体积**：图片文件在磁盘上占用的字节数；它与图片的像素尺寸是两个独立指标。

**尺寸调整**：改变图片输出宽度或高度的操作，以像素为单位；它不等同于文件体积压缩。

**质量参数**：JPEG 或 AVIF 有损编码使用的 `1-100` 参数；数值越高通常保留越多视觉细节，但输出文件也可能更大。

**无损压缩级别**：PNG 编码使用的 `1-9` 参数；只改变编码压缩程度，不改变图片像素内容。

**压缩结果**：一次压缩处理产生的输出文件及其与源文件的体积比较，包括输出大小、节省字节数和压缩比例；输出体积未减少时必须明确标示。
