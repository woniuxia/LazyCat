# LazyCat 工具领域

记录 LazyCat 开发者工具中需要保持一致的用户领域术语。

## 离线微工具路线图

**离线核心路径**：
微工具从用户输入到主要输出的完整路径，在断网、无远程认证、CDN 和在线服务时仍可完成。可选联网增强不属于核心路径。

**维护边界**：
一个微工具在输入输出、格式和依赖上的明确支持范围，以及不在范围内的扩展方向。

**不做**：
在实现前确认核心路径无法离线完成，或问题无法收敛为单点微工具时，对该候选不进入实现。

**停止扩展**：
现有 MVP 仍可用，但新需求超出既定维护边界时，保留现有能力而不继续增加范围。

**退出**：
核心能力因安全、许可证、系统兼容性或依赖失效而无法继续维护时，将该工具从可维护集合移除；低使用量本身不构成退出理由。

**异常堆栈整理器**：
面向常见 JavaScript/TypeScript、Java、Python 和 Rust 异常堆栈的本地整理工具，提取异常类型、消息、文件路径和行列号，生成排查摘要。

**文本编码与换行诊断器**：
面向本地文本和代码文件的编码、BOM、换行符及不可见字符检查工具，并提供明确的常见编码转换预览。

## HTTP 状态码工具

**状态码条目**：HTTP 状态码工具中可被列表或搜索结果选中的一条状态码记录，包含码值、标准名称、中文含义、用途和排查相关信息。

**详情展开**：在状态码列表的原行位置展开该状态码的实战信息，不离开当前列表上下文。

**实战型详情**：状态码条目的扩展说明，包含语义解释、常见场景、触发原因、排查建议和相关响应头提示。

**多重详情展开**：同一列表中允许多个状态码条目同时保持展开；用户收起某一条时不影响其他条目。

**常见标准状态码**：纳入 HTTP 标准定义且在开发排查中常用的状态码集合；不包含厂商或代理实现自定义的非标准码。

**未知状态码**：输入的三位数不在收录的标准状态码条目中时，仅根据 1xx–5xx 段位显示通用分类，并明确标注具体含义未定义；不得生成具体语义或排查建议。

**通用分类提示**：针对未知状态码显示的独立提示区域，说明其所属 1xx–5xx 段位及“具体含义未定义”，不作为正式状态码条目参与列表或详情展开。

## JSON 数组过滤

**JSON 输入文档**：用户提供给 JSON 工作台、作为后续处理唯一事实来源的 JSON 内容。

**数组路径**：JSON 输入文档中实际解析为数组值的 JSONPath；数组过滤器展示并使用按深度优先顺序发现的首个可用数组路径。

**对象数组**：由 JSON 对象元素组成的数组，空数组也归入该形态，是数组过滤器当前支持的目标数组形态。

**属性候选**：对象数组所有元素顶层 key 的按首次出现顺序去重集合；勾选后用于构造数组过滤结果。

**缺失属性**：某个对象元素中不存在的已选属性；数组过滤结果中省略该 key，不用默认值或 `null` 代替。

**默认全选**：数组路径确定后，属性候选初始全部处于选中状态的交互规则。

**空属性投影**：没有选中任何属性时，对象数组中每个元素仍对应结果中的一个空对象，数组长度保持不变。

**结果属性顺序**：数组过滤结果中每个对象沿用对应输入对象的原始 key 顺序，只移除未选属性。

**自动解析**：JSON 输入文档停止编辑约 300ms 后自动解析；空输入清空派生状态，非法输入清空旧结果并显示错误，合法输入立即重新计算首个可用数组。

**解析失败状态**：JSON 输入文档无法解析时保留输入文本，但清空数组路径、属性选择和数组过滤结果，直到下一次解析成功。

**无对象数组空态**：JSON 输入文档语法正确但没有可过滤对象数组时的结果状态；显示说明性提示，不生成路径或结果。

**不支持数组**：包含 primitive、`null`、嵌套数组或对象/非对象混合元素的数组，不作为数组过滤目标。

**首个可用数组路径**：按 JSON 文档深度优先顺序遇到的第一个对象数组路径；primitive/混合数组跳过，空数组可作为首个可用路径。

**数组过滤结果**：基于 JSON 输入文档中首个可用数组路径和用户选择的属性生成的只读根数组投影；不会反向修改 JSON 输入文档。

## 测试邮件助手

**邮件正文模板**：测试邮件助手中可被选择、编辑和保存的一份邮件正文内容；正文中的占位符用于填写字段并生成预览。

**系统名**：用于标识被测试系统的名称，来自 Word 模板变量 `应用系统名称`。

**需求内容**：测试邮件助手中的多行需求文本，来自 Word 模板变量 `功能需求内容`。

**需求号**：从需求内容每个非空行中识别出的需求标识；每行在序号点号后跳过分隔空白，取到需求号后的第一个空白为止，多行需求号以顿号连接。

**测试报告文件名**：由生成日期、固定文本“测试报告”、系统名和需求号组成的 Word 报告名称；同一名称再次生成时覆盖已有报告。

## 工具标签页状态

**工具标签页**：应用主界面中代表一个已打开工具的独立导航项；标签页仍存在时，该工具的工作上下文仍属于当前会话。

**标签页状态保留**：用户从一个工具切换到另一个工具后，仍打开的工具应保留切换前的输入内容、滚动位置和局部操作上下文；关闭标签页才结束该工具页面的会话状态。应用重启后的恢复不属于该术语默认含义。

**标签页滚动上下文**：工具标签页外层页面视口及页面实例内部滚动区域的当前位置；切换标签页时随页面上下文保留，关闭标签页时结束。它不表示应用重启后的滚动快照恢复。

**标签页失活**：工具标签页仍处于打开状态，但暂时不是当前可交互页面；其用户工作状态继续保留，页面级交互和资源活动是否暂停按生命周期契约处理。

**标签页首次呈现**：一个标签页页面上下文第一次向用户展示的阶段；首次呈现时允许使用引导性的视觉动效，若用户在动效完成前离开，后续从失活状态恢复时直接进入可用状态，不等待或重播未完成动效。

**首页内容即时可用**：首页从缓存恢复或内容发生变化时，已有和新出现的工具内容都应立即可见并可交互，不等待整页视觉动效完成；系统要求减少动态效果时，首次呈现也直接可用。

**应用级长任务**：启动后独立于当前工具标签页继续运行的异步工作；切换或关闭标签页不等于取消，用户必须显式终止，后续重新打开相关工具时仍可查看其当前状态和最终结果。

**页面级视图**：负责展示工具状态和承载当前交互的页面上下文；其失活或销毁不应自动改变独立运行的应用级长任务。

**统一标签页缓存**：首页、设置、离线手册和普通工具等所有可重新打开的标签页遵循同一状态保留和关闭销毁契约，不因工具类型不同而改变用户对页面连续性的预期。

**Tab 页面容器**：一个已打开标签页对应的独立页面上下文；它拥有该标签页的 UI 状态和生命周期，标签页关闭时连同其缓存状态一起结束。

**单工具标签页**：同一个工具标识在同一时间最多对应一个打开的标签页；再次打开该工具时激活已有标签页，关闭后重新打开才创建新的页面上下文。

**页面级单次操作**：由用户在工具页面发起、预期产生一次结果的异步操作；切换标签页不取消或重复启动，页面关闭后不再向已销毁页面回写结果。
